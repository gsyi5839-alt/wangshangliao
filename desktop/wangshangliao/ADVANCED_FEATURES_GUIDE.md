# 高级功能使用指南

本文档介绍旺商聊机器人系统的高级功能，这些功能全部来自招财狗(ZCG)软件的逆向解析。

---

## 📋 目录

1. [托管自动下注系统](#托管自动下注系统)
2. [猜数字游戏](#猜数字游戏)
3. [长龙减赔系统](#长龙减赔系统)
4. [夜宵/流水返点](#夜宵流水返点)

---

## 托管自动下注系统

### 功能说明
玩家开启托管后，机器人根据其余额自动选择下注策略并在每期封盘前自动下注。

### 玩家命令

| 命令 | 说明 |
|------|------|
| `托管` | 开启托管(使用自动策略) |
| `托管 da100 x100` | 开启托管(自定义下注内容) |
| `取消托管` / `qxtg` | 取消托管 |
| `托管状态` / `tgzt` | 查询托管状态 |

### 分数段策略配置

系统根据玩家余额自动选择对应的下注策略：

```
┌─────────────┬─────────────────────────────────────┐
│ 余额范围    │ 下注策略示例                         │
├─────────────┼─────────────────────────────────────┤
│ 100-500     │ da20|x22|d24|s26|dad28|das30        │
│ 501-1000    │ da40|x44|d48|s52|dad56|das60        │
│ 1001-5000   │ da200|x220|d240|s260|dad280|das300  │
│ 5001-20000  │ da1120|x1500|d1300|s1200|dad990     │
└─────────────┴─────────────────────────────────────┘
```

### 代码示例

```csharp
// 获取托管配置
var config = TrusteeService.Instance.GetConfig();

// 添加托管玩家
TrusteeService.Instance.AddTrustee(playerId, playerNick, teamId, customBet);

// 移除托管
TrusteeService.Instance.RemoveTrustee(playerId);

// 触发托管下注 (封盘提醒时调用)
TrusteeService.Instance.TriggerAutoBet(teamId, secondsToSeal);
```

### 配置文件
```
C:\wangshangliao\desktop\wangshangliao\data\trustee-config.ini
```

---

## 猜数字游戏

### 功能说明
玩家可以在开奖前猜测开奖号码(0-27)，猜中可获得积分奖励，奖励金额根据余额阶梯计算。

### 玩家命令

| 命令 | 说明 |
|------|------|
| `猜13` | 猜测开奖号码为13 |
| `猜 5 15 25` | 同时猜测多个号码 |

### 奖励规则

```
┌─────────────┬─────────────┐
│ 余额要求    │ 猜中奖励    │
├─────────────┼─────────────┤
│ ≥5000       │ 588         │
│ ≥1000       │ 188         │
│ ≥500        │ 20          │
│ ≥100        │ 8           │
└─────────────┴─────────────┘
```

### 特殊规则
- **禁止猜测的数字:** 13, 14 (可配置)
- **每期最大猜测次数:** 3次 (可配置)

### 代码示例

```csharp
// 尝试猜测
var result = GuessNumberService.Instance.TryGuess(
    playerId, playerNick, "猜13", periodNumber);

if (result.Success)
{
    Console.WriteLine($"已记录猜测: {string.Join(",", result.Numbers)}");
}

// 开奖结算
var winners = GuessNumberService.Instance.Settle(periodNumber, winningNumber);
foreach (var winner in winners)
{
    Console.WriteLine($"{winner.PlayerNick} 猜中，奖励 {winner.Reward}");
}
```

### 配置文件
```
C:\wangshangliao\desktop\wangshangliao\data\guess-number-config.ini
```

---

## 长龙减赔系统

### 功能说明
当某个结果（大/小/单/双）连续开出时，自动降低该结果的赔率，防止庄家亏损。

### 减赔规则

```
┌─────────────┬─────────────┐
│ 连开次数    │ 减少赔率    │
├─────────────┼─────────────┤
│ 3次         │ 0.1         │
│ 6次         │ 0.2         │
│ 9次         │ 0.3         │
│ 12次        │ 0.4         │
└─────────────┴─────────────┘
```

### 跟踪分类
- **大小**: 大、小
- **单双**: 单、双
- **组合**: 大单、大双、小单、小双

### 代码示例

```csharp
// 记录开奖结果
DragonReduceService.Instance.RecordResult(sum, bigSmall, oddEven, special);

// 获取调整后的赔率
decimal adjustedOdds = DragonReduceService.Instance.GetAdjustedOdds("大小", "大", 1.8m);

// 查询当前长龙
var dragons = DragonReduceService.Instance.GetCurrentDragons();
foreach (var dragon in dragons)
{
    Console.WriteLine($"{dragon.Category}-{dragon.Result} 连开{dragon.ConsecutiveCount}次");
}

// 获取历史记录字符串
string history = DragonReduceService.Instance.GetHistoryString("大小", 20);
// 输出: 大小大大大小小大小大...
```

### 配置文件
```
C:\wangshangliao\desktop\wangshangliao\data\dragon-reduce-config.ini
```

---

## 夜宵/流水返点

### 功能说明
根据玩家的流水(下注总额)或输赢情况，发放奖励或返点。

### 玩家命令

| 命令 | 说明 |
|------|------|
| `反水` / `返水` / `回水` | 申请流水返点 |
| `夜宵` / `领夜宵` | 申请夜宵奖励 |
| `流水` / `我的流水` | 查询今日流水 |

### 计算方式

#### 1. 按把数 (夜宵)
```
┌─────────────────────┬─────────┬─────────┐
│ 流水范围            │ 把数要求 │ 奖励    │
├─────────────────────┼─────────┼─────────┤
│ 30001-50000         │ 30把    │ 2888    │
│ 50001-100000        │ 50把    │ 5888    │
│ 100001-500000       │ 100把   │ 8888    │
└─────────────────────┴─────────┴─────────┘
```

#### 2. 按输赢 (夜宵)
```
赢钱规则:
  赢 ≥280000 → 奖励888
  赢 ≥500000 → 奖励1888

输钱规则:
  输 ≥280000 → 奖励888
  输 ≥500000 → 奖励1888
```

#### 3. 按流水 (返点)
```
┌─────────────────┬─────────────┐
│ 流水金额        │ 返点比例    │
├─────────────────┼─────────────┤
│ ≥10000          │ 0.5%        │
│ ≥50000          │ 1.0%        │
│ ≥100000         │ 1.5%        │
└─────────────────┴─────────────┘
```

### 代码示例

```csharp
// 计算流水返点
var result = BonusService.Instance.CalculateTurnoverRebate(playerId, playerNick);
if (result.Success)
{
    Console.WriteLine($"返点金额: {result.RebateAmount}");
}

// 计算夜宵奖励 (按把数)
var nightSnack = BonusService.Instance.CalculateNightSnack(playerId, playerNick);

// 计算夜宵奖励 (按输赢)
var nightSnackWL = BonusService.Instance.CalculateNightSnackByWinLose(playerId, playerNick);

// 检查是否为返点命令
if (BonusService.Instance.IsRebateCommand(message))
{
    // 处理返点
}
```

### 回复模板变量
| 变量 | 说明 |
|------|------|
| `[艾特]` | @玩家 |
| `[旺旺]` | 玩家昵称 |
| `[分数]` | 返点金额 |
| `[余粮]` | 当前余额 |
| `[把数]` | 把数要求 |
| `[换行]` | 换行符 |

### 配置文件
```
C:\wangshangliao\desktop\wangshangliao\data\bonus-config.ini
```

---

## UI控件使用

### 托管设置控件
```csharp
// 在窗体中添加托管设置控件
var trusteeControl = new TrusteeSettingsControl();
trusteeControl.Dock = DockStyle.Fill;
this.Controls.Add(trusteeControl);
```

### 返点设置控件
```csharp
// 在窗体中添加返点设置控件
var bonusControl = new BonusSettingsControl();
bonusControl.Dock = DockStyle.Fill;
this.Controls.Add(bonusControl);
```

---

## 完整集成示例

```csharp
// 启动机器人 (自动集成所有高级功能)
var bot = BotController.Instance;

// 绑定事件
bot.OnLog += msg => Console.WriteLine(msg);
bot.OnDepositRequest += (pid, amount, balance) => 
    Console.WriteLine($"上分请求: {pid} {amount}");
bot.OnWithdrawRequest += (pid, amount, balance) => 
    Console.WriteLine($"下分请求: {pid} {amount}");

// 启动
await bot.StartAsync("群ID");

// 处理消息 (自动分发到各处理器)
await bot.ProcessMessageAsync(message, "群ID", "发送者ID", "发送者昵称");

// 停止
bot.Stop();
```

---

## 文件结构

```
Services/
├── TrusteeService.cs       # 托管自动下注服务
├── GuessNumberService.cs   # 猜数字游戏服务
├── DragonReduceService.cs  # 长龙减赔服务
├── BonusService.cs         # 夜宵/流水返点服务
└── Bot/
    └── Handlers/
        ├── TrusteeHandler.cs      # 托管命令处理器
        ├── GuessNumberHandler.cs  # 猜数字命令处理器
        └── BonusHandler.cs        # 返点命令处理器

Controls/
├── TrusteeSettingsControl.cs   # 托管设置UI
└── BonusSettingsControl.cs     # 返点设置UI
```

---

## 来源说明

所有功能均来自招财狗(ZCG) v4.25软件的完整逆向解析：

- **配置解密:** XOR 0x10 + GB2312编码
- **原始配置文件:** `zcg\设置.ini`
- **功能提取:** 73个开关配置 + 117个参数配置
- **完整报告:** `C:\Users\Administrator\Desktop\zcg25.12.11\逆向分析结果\招财狗完整功能分析报告.md`

---

*文档更新时间: 2026-01-10*
